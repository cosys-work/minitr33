import { VisNetDataDirective } from './vis-net-data.directive';

describe('VisNetDataDirective', () => {
  it('should create an instance', () => {
    // const directive = new VisNetDataDirective();
    // expect(directive).toBeTruthy();
  });
});
